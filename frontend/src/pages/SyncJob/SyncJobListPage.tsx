/** Sync Job list page — placeholder */

import { Typography } from 'antd';

const { Title } = Typography;

export default function SyncJobListPage() {
  return (
    <div>
      <Title level={4}>Sync Jobs</Title>
      <p>Sync job management — coming in Phase 1.4</p>
    </div>
  );
}
