/** Data Source list page — placeholder */

import { Typography } from 'antd';

const { Title } = Typography;

export default function DataSourceListPage() {
  return (
    <div>
      <Title level={4}>Data Sources</Title>
      <p>Data source management — coming in Phase 1.3</p>
    </div>
  );
}
